# kleine
A cross-platform, audio-enabled C++ console code project, using CMake and Klang.

## WebAssembly Build
The Emscripten SDK is expected under `include/emsdk`.
Use the `emscripten-debug` CMake preset to configure and build.
