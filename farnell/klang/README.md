# Klang models from Designing Sound

Develop the first ports here from chapter 25 (phone tones) and chapter 29 (telephone bell), following the comparison process in [AGENTS.md](../../AGENTS.md).

Record each model's chapter/figure, source patch, controls, render recipe, and comparison findings. Existing machine ports used for calibration are `ToyBoatEngine` and `FourStrokeEngine` in [sounds/Motors.h](../../sounds/Motors.h).
